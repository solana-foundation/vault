/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VAULT_DISCRIMINATOR = new Uint8Array([
  211, 8, 232, 43, 2, 152, 117, 119,
]);

export function getVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DISCRIMINATOR);
}

export type Vault = {
  discriminator: ReadonlyUint8Array;
  assetMint: Address;
  /** share mint address */
  shareMint: Address;
  /** token account holding confirmed vault assets */
  vaultTokenAccount: Address;
  /** authority that can sign permissioned instructions */
  authority: Address;
  /** pubkey that is required to own the TokenAccount fees are sent to */
  feeRecipient: Address;
  /** paused */
  paused: boolean;
  /** once a vault is initialized, no extensions can be added */
  initialized: boolean;
  /** token account holding assets from deposits awaiting share issuance */
  pendingVault: Address;
  /** net asset value (assets per share), default 0 until first NAV update */
  nav: bigint;
  /** nav version, incremented on each NAV update */
  navVersion: bigint;
  /** count of pending async deposit/withdrawal requests */
  pendingAsyncRequests: number;
  /**
   * virtual vault asset balance, accounts for tokens that may
   * have been withdrawn by the vault authority
   */
  totalAssetBalance: bigint;
  reserveBump: number;
  pendingVaultBump: number;
  bump: number;
  pendingAuthority: Option<Address>;
};

export type VaultArgs = {
  assetMint: Address;
  /** share mint address */
  shareMint: Address;
  /** token account holding confirmed vault assets */
  vaultTokenAccount: Address;
  /** authority that can sign permissioned instructions */
  authority: Address;
  /** pubkey that is required to own the TokenAccount fees are sent to */
  feeRecipient: Address;
  /** paused */
  paused: boolean;
  /** once a vault is initialized, no extensions can be added */
  initialized: boolean;
  /** token account holding assets from deposits awaiting share issuance */
  pendingVault: Address;
  /** net asset value (assets per share), default 0 until first NAV update */
  nav: number | bigint;
  /** nav version, incremented on each NAV update */
  navVersion: number | bigint;
  /** count of pending async deposit/withdrawal requests */
  pendingAsyncRequests: number;
  /**
   * virtual vault asset balance, accounts for tokens that may
   * have been withdrawn by the vault authority
   */
  totalAssetBalance: number | bigint;
  reserveBump: number;
  pendingVaultBump: number;
  bump: number;
  pendingAuthority: OptionOrNullable<Address>;
};

/** Gets the encoder for {@link VaultArgs} account data. */
export function getVaultEncoder(): Encoder<VaultArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["assetMint", getAddressEncoder()],
      ["shareMint", getAddressEncoder()],
      ["vaultTokenAccount", getAddressEncoder()],
      ["authority", getAddressEncoder()],
      ["feeRecipient", getAddressEncoder()],
      ["paused", getBooleanEncoder()],
      ["initialized", getBooleanEncoder()],
      ["pendingVault", getAddressEncoder()],
      ["nav", getU128Encoder()],
      ["navVersion", getU64Encoder()],
      ["pendingAsyncRequests", getU16Encoder()],
      ["totalAssetBalance", getU64Encoder()],
      ["reserveBump", getU8Encoder()],
      ["pendingVaultBump", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["pendingAuthority", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: VAULT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Vault} account data. */
export function getVaultDecoder(): Decoder<Vault> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["assetMint", getAddressDecoder()],
    ["shareMint", getAddressDecoder()],
    ["vaultTokenAccount", getAddressDecoder()],
    ["authority", getAddressDecoder()],
    ["feeRecipient", getAddressDecoder()],
    ["paused", getBooleanDecoder()],
    ["initialized", getBooleanDecoder()],
    ["pendingVault", getAddressDecoder()],
    ["nav", getU128Decoder()],
    ["navVersion", getU64Decoder()],
    ["pendingAsyncRequests", getU16Decoder()],
    ["totalAssetBalance", getU64Decoder()],
    ["reserveBump", getU8Decoder()],
    ["pendingVaultBump", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["pendingAuthority", getOptionDecoder(getAddressDecoder())],
  ]);
}

/** Gets the codec for {@link Vault} account data. */
export function getVaultCodec(): Codec<VaultArgs, Vault> {
  return combineCodec(getVaultEncoder(), getVaultDecoder());
}

export function decodeVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Vault, TAddress> | MaybeAccount<Vault, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVaultDecoder(),
  );
}

export async function fetchVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Vault, TAddress>> {
  const maybeAccount = await fetchMaybeVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Vault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVault(maybeAccount);
}

export async function fetchAllVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Vault>[]> {
  const maybeAccounts = await fetchAllMaybeVault(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Vault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVault(maybeAccount));
}
