/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ASYNC_VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getFeeTypeDecoder,
  getFeeTypeEncoder,
  type FeeType,
  type FeeTypeArgs,
} from "../types";

export const UPDATE_DEPOSIT_FEE_DISCRIMINATOR = new Uint8Array([
  60, 169, 133, 28, 78, 234, 0, 31,
]);

export function getUpdateDepositFeeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_DEPOSIT_FEE_DISCRIMINATOR,
  );
}

export type UpdateDepositFeeInstruction<
  TProgram extends string = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateDepositFeeInstructionData = {
  discriminator: ReadonlyUint8Array;
  newDepositFee: FeeType;
};

export type UpdateDepositFeeInstructionDataArgs = {
  newDepositFee: FeeTypeArgs;
};

export function getUpdateDepositFeeInstructionDataEncoder(): Encoder<UpdateDepositFeeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newDepositFee", getFeeTypeEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_DEPOSIT_FEE_DISCRIMINATOR }),
  );
}

export function getUpdateDepositFeeInstructionDataDecoder(): Decoder<UpdateDepositFeeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newDepositFee", getFeeTypeDecoder()],
  ]);
}

export function getUpdateDepositFeeInstructionDataCodec(): Codec<
  UpdateDepositFeeInstructionDataArgs,
  UpdateDepositFeeInstructionData
> {
  return combineCodec(
    getUpdateDepositFeeInstructionDataEncoder(),
    getUpdateDepositFeeInstructionDataDecoder(),
  );
}

export type UpdateDepositFeeInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault: Address<TAccountVault>;
  newDepositFee: UpdateDepositFeeInstructionDataArgs["newDepositFee"];
};

export function getUpdateDepositFeeInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateDepositFeeInput<TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): UpdateDepositFeeInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? ASYNC_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
    ],
    data: getUpdateDepositFeeInstructionDataEncoder().encode(
      args as UpdateDepositFeeInstructionDataArgs,
    ),
    programAddress,
  } as UpdateDepositFeeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault
  >);
}

export type ParsedUpdateDepositFeeInstruction<
  TProgram extends string = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: UpdateDepositFeeInstructionData;
};

export function parseUpdateDepositFeeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateDepositFeeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), vault: getNextAccount() },
    data: getUpdateDepositFeeInstructionDataDecoder().decode(instruction.data),
  };
}
