/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ASYNC_VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const UPDATE_MIN_SUBSCRIPTION_DISCRIMINATOR = new Uint8Array([
  63, 126, 170, 249, 163, 30, 203, 167,
]);

export function getUpdateMinSubscriptionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_MIN_SUBSCRIPTION_DISCRIMINATOR,
  );
}

export type UpdateMinSubscriptionInstruction<
  TProgram extends string = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMinSubscriptionInstructionData = {
  discriminator: ReadonlyUint8Array;
  threshold: bigint;
};

export type UpdateMinSubscriptionInstructionDataArgs = {
  threshold: number | bigint;
};

export function getUpdateMinSubscriptionInstructionDataEncoder(): FixedSizeEncoder<UpdateMinSubscriptionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["threshold", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_MIN_SUBSCRIPTION_DISCRIMINATOR,
    }),
  );
}

export function getUpdateMinSubscriptionInstructionDataDecoder(): FixedSizeDecoder<UpdateMinSubscriptionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["threshold", getU64Decoder()],
  ]);
}

export function getUpdateMinSubscriptionInstructionDataCodec(): FixedSizeCodec<
  UpdateMinSubscriptionInstructionDataArgs,
  UpdateMinSubscriptionInstructionData
> {
  return combineCodec(
    getUpdateMinSubscriptionInstructionDataEncoder(),
    getUpdateMinSubscriptionInstructionDataDecoder(),
  );
}

export type UpdateMinSubscriptionInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault: Address<TAccountVault>;
  threshold: UpdateMinSubscriptionInstructionDataArgs["threshold"];
};

export function getUpdateMinSubscriptionInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateMinSubscriptionInput<TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): UpdateMinSubscriptionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? ASYNC_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
    ],
    data: getUpdateMinSubscriptionInstructionDataEncoder().encode(
      args as UpdateMinSubscriptionInstructionDataArgs,
    ),
    programAddress,
  } as UpdateMinSubscriptionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault
  >);
}

export type ParsedUpdateMinSubscriptionInstruction<
  TProgram extends string = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
  };
  data: UpdateMinSubscriptionInstructionData;
};

export function parseUpdateMinSubscriptionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateMinSubscriptionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), vault: getNextAccount() },
    data: getUpdateMinSubscriptionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
