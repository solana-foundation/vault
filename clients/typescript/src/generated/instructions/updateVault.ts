/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findVaultPda } from "../pdas";
import { ASYNC_VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const UPDATE_VAULT_DISCRIMINATOR = new Uint8Array([
  67, 229, 185, 188, 226, 11, 210, 60,
]);

export function getUpdateVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_VAULT_DISCRIMINATOR,
  );
}

export type UpdateVaultInstruction<
  TProgram extends string = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountShareMint extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountShareMint extends string
        ? ReadonlyAccount<TAccountShareMint>
        : TAccountShareMint,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  paused: Option<boolean>;
  feeRecipient: Option<Address>;
};

export type UpdateVaultInstructionDataArgs = {
  paused: OptionOrNullable<boolean>;
  feeRecipient: OptionOrNullable<Address>;
};

export function getUpdateVaultInstructionDataEncoder(): Encoder<UpdateVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["paused", getOptionEncoder(getBooleanEncoder())],
      ["feeRecipient", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_VAULT_DISCRIMINATOR }),
  );
}

export function getUpdateVaultInstructionDataDecoder(): Decoder<UpdateVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["paused", getOptionDecoder(getBooleanDecoder())],
    ["feeRecipient", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getUpdateVaultInstructionDataCodec(): Codec<
  UpdateVaultInstructionDataArgs,
  UpdateVaultInstructionData
> {
  return combineCodec(
    getUpdateVaultInstructionDataEncoder(),
    getUpdateVaultInstructionDataDecoder(),
  );
}

export type UpdateVaultAsyncInput<
  TAccountAuthority extends string = string,
  TAccountShareMint extends string = string,
  TAccountVault extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  shareMint: Address<TAccountShareMint>;
  vault?: Address<TAccountVault>;
  paused: UpdateVaultInstructionDataArgs["paused"];
  feeRecipient: UpdateVaultInstructionDataArgs["feeRecipient"];
};

export async function getUpdateVaultInstructionAsync<
  TAccountAuthority extends string,
  TAccountShareMint extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateVaultAsyncInput<
    TAccountAuthority,
    TAccountShareMint,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountShareMint,
    TAccountVault
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ASYNC_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    shareMint: { value: input.shareMint ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      shareMint: expectAddress(accounts.shareMint.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.shareMint),
      getAccountMeta(accounts.vault),
    ],
    data: getUpdateVaultInstructionDataEncoder().encode(
      args as UpdateVaultInstructionDataArgs,
    ),
    programAddress,
  } as UpdateVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountShareMint,
    TAccountVault
  >);
}

export type UpdateVaultInput<
  TAccountAuthority extends string = string,
  TAccountShareMint extends string = string,
  TAccountVault extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  shareMint: Address<TAccountShareMint>;
  vault: Address<TAccountVault>;
  paused: UpdateVaultInstructionDataArgs["paused"];
  feeRecipient: UpdateVaultInstructionDataArgs["feeRecipient"];
};

export function getUpdateVaultInstruction<
  TAccountAuthority extends string,
  TAccountShareMint extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
>(
  input: UpdateVaultInput<TAccountAuthority, TAccountShareMint, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): UpdateVaultInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountShareMint,
  TAccountVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? ASYNC_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    shareMint: { value: input.shareMint ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.shareMint),
      getAccountMeta(accounts.vault),
    ],
    data: getUpdateVaultInstructionDataEncoder().encode(
      args as UpdateVaultInstructionDataArgs,
    ),
    programAddress,
  } as UpdateVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountShareMint,
    TAccountVault
  >);
}

export type ParsedUpdateVaultInstruction<
  TProgram extends string = typeof ASYNC_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    shareMint: TAccountMetas[1];
    vault: TAccountMetas[2];
  };
  data: UpdateVaultInstructionData;
};

export function parseUpdateVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      shareMint: getNextAccount(),
      vault: getNextAccount(),
    },
    data: getUpdateVaultInstructionDataDecoder().decode(instruction.data),
  };
}
